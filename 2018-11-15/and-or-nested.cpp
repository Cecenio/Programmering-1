#include <iostream>
using namespace std;
int main()
{
    // Om "AND", "OR" och "icke"
	// Om && (och)
	// || (eller)
	// ! (icke)
	
	int a, b, c;
	cout << "ange tre heltal   :>"
	cin >> a;
	cin >> b;
	
	if ( (a<b) && (b<C) )
	cout << "Stämmer" << endl;
	
	if (a < b)
	{
		if (b < c)
		 cout << "Stämmer också" << endl;
	}
	
	return 0;
}
